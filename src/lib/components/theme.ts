export const SITE_TITLE = "The Labyrinth of Polyominoes"

export function pageTitle(title: string) {
  return `${title} ▪ ${SITE_TITLE}`
}

export const colors = {
  // 4-color palette used for stuff
  palette: ["#ff4400", "#ffcc00", "#00ff99", "#3cf"],
}
