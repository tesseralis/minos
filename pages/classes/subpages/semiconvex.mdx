A fixed polyomino is _row convex_ if a horizontal line through each row only goes through one component. It is _column convex_ if a vertical line through each column goes through one component. We call a free polyomino _semiconvex_ if it is either row convex or column convex.
